/** @type {import('next').NextConfig} */
const nextConfig : import('next').NextConfig = {
  images: {
    domains: ['i.pravatar.cc', 'placehold.co', 'images.unsplash.com']
  },
}

module.exports = nextConfig
